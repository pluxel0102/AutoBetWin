<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AutoBet.Views.MelBetSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop/>
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кастомный заголовок окна -->
        <Grid Grid.Row="0"
              Height="40"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Область для перетаскивания -->
            <Grid x:Name="AppTitleBar" 
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  Background="Transparent">
                
                <!-- Иконка и название окна -->
                <StackPanel Orientation="Horizontal" 
                           Spacing="8" 
                           Margin="16,0,0,0"
                           VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE713;" 
                             FontSize="16"
                             Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Настройки режима МелБет" 
                              FontSize="13"
                              FontWeight="SemiBold"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Кнопка закрытия -->
            <!-- Кнопка закрытия убрана, используются только системные кнопки -->
        </Grid>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" Padding="20">
            <StackPanel Spacing="20">
                
                <!-- Уведомление о сохранении -->
                <InfoBar x:Name="SaveNotificationBar"
                        Title="Изменения сохранены автоматически"
                        Severity="Success"
                        IsOpen="False"
                        IsClosable="True"
                        CornerRadius="12"
                        Margin="0,0,0,10"
                        Opacity="0">
                    <InfoBar.IconSource>
                        <SymbolIconSource Symbol="Accept"/>
                    </InfoBar.IconSource>
                    <InfoBar.Shadow>
                        <ThemeShadow/>
                    </InfoBar.Shadow>
                    <InfoBar.Resources>
                        <x:Double x:Key="InfoBarMinHeight">48</x:Double>
                    </InfoBar.Resources>
                </InfoBar>

                <!-- Начальная ставка -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Начальная ставка для режима МелБет"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Выберите начальную сумму ставки при запуске игры"
                              FontSize="12"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                              TextWrapping="Wrap"/>
                    <ComboBox x:Name="BaseBetComboBox"
                             SelectionChanged="BaseBetComboBox_SelectionChanged"
                             HorizontalAlignment="Stretch"
                             Margin="0,8,0,0">
                        <ComboBoxItem Content="10" Tag="10"/>
                        <ComboBoxItem Content="20" Tag="20"/>
                        <ComboBoxItem Content="50" Tag="50"/>
                        <ComboBoxItem Content="100" Tag="100"/>
                        <ComboBoxItem Content="500" Tag="500"/>
                        <ComboBoxItem Content="1000" Tag="1000"/>
                        <ComboBoxItem Content="2000" Tag="2000"/>
                        <ComboBoxItem Content="5000" Tag="5000"/>
                        <ComboBoxItem Content="10000" Tag="10000"/>
                        <ComboBoxItem Content="20000" Tag="20000"/>
                    </ComboBox>
                </StackPanel>

                <!-- Разделитель -->
                <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

                <!-- Стратегия управления ставками -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Стратегия управления ставками"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Выберите стратегию для управления размером ставок после проигрышей"
                              FontSize="12"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                              TextWrapping="Wrap"/>
                    <ComboBox x:Name="StrategyComboBox"
                             SelectionChanged="StrategyComboBox_SelectionChanged"
                             HorizontalAlignment="Stretch"
                             Margin="0,8,0,0">
                        <ComboBoxItem Content="📈 Мартингейл (удвоение после каждого проигрыша)" Tag="Martingale"/>
                        <ComboBoxItem Content="🪜 Лесенка (с задержками на уровнях)" Tag="Ladder"/>
                    </ComboBox>
                </StackPanel>

                <!-- Разделитель -->
                <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

                <!-- Предпочитаемый цвет -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Предпочитаемый цвет для ставки в режиме МелБет"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Выберите цвет, на который будут делаться ставки"
                              FontSize="12"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                              TextWrapping="Wrap"/>
                    <ComboBox x:Name="PreferredColorComboBox"
                             SelectionChanged="PreferredColorComboBox_SelectionChanged"
                             HorizontalAlignment="Stretch"
                             Margin="0,8,0,0">
                        <ComboBoxItem Content="🔵 Синий" Tag="Blue"/>
                        <ComboBoxItem Content="🔴 Красный" Tag="Red"/>
                    </ComboBox>
                </StackPanel>

                <!-- Разделитель -->
                <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

                <!-- Автоматическая смена цвета -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Автоматическая смена цвета после проигрышей в режиме МелБет"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Выберите количество проигрышей подряд, после которых цвет будет автоматически меняться на противоположный"
                              FontSize="12"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                              TextWrapping="Wrap"/>
                    <ComboBox x:Name="ColorSwitchComboBox"
                             SelectionChanged="ColorSwitchComboBox_SelectionChanged"
                             HorizontalAlignment="Stretch"
                             Margin="0,8,0,0">
                        <ComboBoxItem Content="Через 1 проигрыш" Tag="1"/>
                        <ComboBoxItem Content="Через 2 проигрыша" Tag="2" IsSelected="True"/>
                        <ComboBoxItem Content="Через 3 проигрыша" Tag="3"/>
                        <ComboBoxItem Content="Через 4 проигрыша" Tag="4"/>
                        <ComboBoxItem Content="Через 5 проигрышей" Tag="5"/>
                    </ComboBox>
                </StackPanel>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
